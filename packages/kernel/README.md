# @ai-founder-os/kernel

Kernel package for AI Founder OS — Genesis Alpha.

Provides the central kernel that all modules must interact with. Follow the rule: "No module may bypass the Kernel."

Usage:

```ts
import { kernel } from '@ai-founder-os/kernel';
kernel.initialize();
```

Build:

- npm run build (inside packages/kernel)

